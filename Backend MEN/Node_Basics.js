/*
js source code --> V8 --> bytecode --> machine code
--> Run by CPU

package.json -> list of packages with version
 
package-lock.json -> detailed of every package installed with version, sub-dependencies 

// Nodemon

// var fs= require('fs')
// var os = require('os')
console.log(os)
console.log(fs)


  //import file 
  const file=require('./file')

  
  //Export
  module.exports = {
   obj1,
   obj2
  }

  npm i lodash
  const _ = require('lodash')

  npm i prompt-sync
  var prompt = require('prompt-sync')();
  let age = prompt("Enter Your Age ")
  console.log("Age is " + age);
  console.log(typeof age);  //String


  menu card == API
  option in list == Endpoint
*/